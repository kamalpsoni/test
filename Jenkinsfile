pipeline {
  agent any
  stages {
    stage('Git checkout') {
      steps {
        sh 'kubectl get node'
      }
    }
    tage('Helm command') {
      steps {
        sh 'helm list'
      }
    }

  }
}
